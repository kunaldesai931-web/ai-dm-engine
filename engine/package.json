{
  "name": "ai-dm-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic tabletop engine. Code owns the dice and state; the LLM narrates.",
  "scripts": {
    "engine": "tsx src/cli.ts",
    "realm": "tsx src/realm/cli.ts",
    "warband": "tsx src/warband/cli.ts",
    "warband-serve": "tsx src/warband/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "tsx": "^4.22.3",
    "typescript": "^6.0.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1"
  }
}
